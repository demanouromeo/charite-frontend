import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dmsacad.charite',
  appName: 'GSB La Charité',
  webDir: 'dist/frontend/browser'
};

export default config;
